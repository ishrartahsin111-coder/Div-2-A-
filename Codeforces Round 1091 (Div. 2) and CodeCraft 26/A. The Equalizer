#include<bits/stdc++.h>
using namespace std;
int main()
{
    int t;
    cin>>t;
    while(t--)
    {
        int n,k;
        cin>>n>>k;
        int sum=0;
        int t=n*k;
        for(int i=0;i<n;i++)
        {
            int v;
            cin>>v;
            sum+=v;
        }
        if(sum%2==0 && t%2==1)
        cout<<"NO"<<endl;
        else
        cout<<"YES"<<endl;
    }

}
